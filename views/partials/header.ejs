<!DOCTYPE html>
<html>
<head>
  <style>
nav { display: flex; gap: 20px; padding: 10px; background: #2c3e50; color: #ecf0f1; }
nav a, .dropdown-btn { color: inherit; text-decoration: none; cursor: pointer; color:black }
nav a{
  color: rgb(144, 141, 141);
}
.dropdown { position: relative; }
.dropdown-content {
  display: none;
  position: absolute;
  background: #ecf0f1;
  color: #2c3e50;
  min-width: 120px;
  box-shadow: 0 2px 5px rgba(0,0,0,0.2);
}
.dropdown-content a {
  display: block;
  padding: 8px;
}
.dropdown:hover .dropdown-content { display: block; }
</style>
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/">JobPortal</a>
    <!-- Hamburger / Toggler Button -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item"><a class="nav-link" href="/jobs">Jobs</a></li>
        <nav>
  <% if (!user) { %>
    <a href="/login">Login</a>
    <a href="/register">Register</a>
  <% } else { %>
    <div class="dropdown">
      <button class="dropdown-btn">👤 <%= user.name %></button>
      <div class="dropdown-content">
        <a href="/<%= user.role === 'employer' ? 'employer/dashboard' : (user.role === 'job_seeker' ? 'seeker/dashboard' : 'admin/dashboard') %>">
          Dashboard
        </a>
        <!-- <a href="/logout">Logout</a> -->
          <form action="/logout" method="POST">
        <button class="btn btn-danger mt-3 w-100">Logout</button>
      </form>
      </div>
    </div>
  <% } %>
</nav>
      </ul>
    </div>
  </div>
</nav>
<div class="container mt-4">

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>